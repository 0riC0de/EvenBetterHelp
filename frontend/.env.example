# Fictional in-memory demo. Set false for the real backend.
NEXT_PUBLIC_DEMO_MODE=true
BACKEND_URL=http://localhost:8080
# Only false on local HTTP; HTTPS deployments must use true.
COOKIE_SECURE=false
